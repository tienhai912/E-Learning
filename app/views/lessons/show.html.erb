<% provide :title, @lesson.name %>

<h2 class="lesson-name">
  <%= t "words_in" %>
  <%= link_to @lesson.name, course_lesson_path(@lesson.course_id, @lesson.id)%>
</h2>
<ul class="words">
  <%= render @words %>
</ul>
<%= link_to t("start_lesson"), tests_path(lesson_id: @lesson.id),
  method: :post, class: "btn btn-primary" %>
