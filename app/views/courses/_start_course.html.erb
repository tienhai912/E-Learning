<%= form_for(current_user.enrollments.build) do |f| %>
  <div>
    <%= hidden_field_tag :user_id, current_user.id %>
    <%= hidden_field_tag :course_id, course.id %>
  </div>
  <%= f.submit t("start"), class: "btn btn-primary category-submit" %>
<% end %>
