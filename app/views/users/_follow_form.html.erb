<% unless current_user == user %>
  <div id="follow_form">
  <% if current_user.following? user %>
    <%= render "users/unfollow", user: user, destroy: destroy %>
  <% else %>
    <%= render "users/follow", user: user, build: build %>
  <% end %>
  </div>
<% end %>
