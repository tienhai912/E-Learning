<% provide :title, "Category: #{@category.name}" %>
<span class="category-name"><%= link_to @category.name, @category%></span>
<ul class="courses">
  <%= render @courses %>
</ul>

<%= will_paginate @courses %>
